package org.example.fashionstoresystem.service.user;

public interface UserService {
    // Tạm thời để trống. 
    // Các logic về UserRepository (save, existsByEmail) đã được 
    // AuthServiceImpl gọi trực tiếp (theo đúng sơ đồ CD01).
    // Các hàm của UserService sẽ liên quan đến đổi mật khẩu, cập nhật profile... 
    // (sẽ làm khi code tới các Use Case khác).
}
